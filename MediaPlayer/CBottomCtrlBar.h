#pragma once
#include <QWidget>
#include <QObject>

class CBottomCtrlBar : public QWidget
{
	Q_OBJECT

public:
	CBottomCtrlBar(QWidget* p = nullptr);
	~CBottomCtrlBar();
private:

private slots:

signals:

private:
};

