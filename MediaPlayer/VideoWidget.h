#pragma once
#include <QWidget>
#include <QObject>


class VideoWidget : public QWidget
{
	Q_OBJECT

public:
	VideoWidget(QWidget* p = nullptr);
	~VideoWidget();

private:

private slots:

signals:

private:


};


